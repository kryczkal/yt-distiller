{
  "name": "yt-distill-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local backend: YouTube video -> transcript -> Claude distillation. Runs on Claude subscription via the Agent SDK.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "mcp": "node mcp-server.mjs",
    "smoke": "node test/auth-smoke.mjs",
    "smoke:transcript": "node test/transcript-test.mjs",
    "smoke:distill": "node test/distill-test.mjs",
    "smoke:mcp": "node test/mcp-test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.158",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
